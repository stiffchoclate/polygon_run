import pygame

#'Characters' represnt the sprite that players can control.
class Character(pygame.sprite.Sprite):
    def __init__(self, center, x, y, image):
        pygame.sprite.Sprite()
        self.center = (x,y)
        self.x
        self.y
